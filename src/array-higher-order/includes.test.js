'use strict'

const includes = require('./includes')

describe('includes()', () => {
  xit('returns `true` if an element is in a list', () => {})
  xit('returns `false` if an element is not in a list', () => {})
  xit('returns `false` for the empty list', () => {})
  xit('is curried', () => {})
})
