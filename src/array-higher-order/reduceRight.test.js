'use strict'

const reduceRight = require('./reduceRight')

describe('reduceRight()', () => {
  xit('folds lists in the right order', () => {})
  xit('returns the accumulator for an empty array', () => {})
  xit('is curried', () => {})
})
