'use strict'

const find = require('./find')

describe('find()', () => {
  xit('returns the first element that satisfies the predicate', () => {})
  xit('returns `undefined` without at match', () => {})
  xit('returns `undefined` when given an empty list', () => {})
  xit('is curried', () => {})
})
