'use strict'

const reduce = require('./reduce')

describe('reduce()', () => {
  xit('folds functions over arrays with the supplied accumulator', () => {})
  xit('returns the accumulator for an empty array', () => {})
  xit('is curried', () => {})
})
