'use strict'

const subtract = require('./subtract')

describe('subtract()', () => {
  xit('subtracts two numbers', () => {})
  xit('coerces its arguments to numbers', () => {})
  xit('is curried', () => {})
})
