'use strict'

const mean = require('./mean')

describe('mean()', () => {
  xit('returns mean of a nonempty list', () => {})
  xit('returns NaN for an empty list', () => {})
  xit('is curried', () => {})
})
