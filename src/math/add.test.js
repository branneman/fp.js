'use strict'

const add = require('./add')

describe('add()', () => {
  xit('adds together two numbers', () => {})
  xit('coerces its arguments to numbers', () => {})
  xit('is curried', () => {})
})
