'use strict'

const median = require('./median')

describe('median()', () => {
  xit('returns middle value of an odd-length list', () => {})
  xit('returns mean of two middle values of a nonempty even-length list', () => {})
  xit('returns NaN for an empty list', () => {})
  xit('is curried', () => {})
})
