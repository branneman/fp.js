'use strict'

const pipe = require('./pipe')

describe('pipe()', () => {
  xit('is a variadic function', () => {})
  xit('performs left-to-right function composition', () => {})
  xit('throws if given no arguments', () => {})
  xit('can be applied to one argument', () => {})
  xit('is curried', () => {})
})
