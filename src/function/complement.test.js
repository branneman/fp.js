'use strict'

const complement = require('./complement')

describe('complement()', () => {
  xit('creates boolean-returning function that reverses another', () => {})
  xit('accepts a function that take multiple parameters', () => {})
  xit('is curried', () => {})
})
