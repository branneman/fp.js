'use strict'

const apply = require('./apply')

describe('apply()', () => {
  xit('applies function to argument list', () => {})
  xit("doesn't have a context", () => {})
  xit('is curried', () => {})
})
