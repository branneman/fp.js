'use strict'

const values = require('./values')

describe('values()', () => {
  xit("returns an array of the given object's values", () => {})
  xit("does not include the given object's prototype properties", () => {})
  xit('returns an empty object for primitives', () => {})
  xit('is curried', () => {})
})
