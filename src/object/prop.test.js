'use strict'

const prop = require('./prop')

describe('prop()', () => {
  xit('returns a function that fetches the appropriate property', () => {})
  xit('returns `undefined` for a missing property', () => {})
  xit('is immutable', () => {})
  xit('is curried', () => {})
})
