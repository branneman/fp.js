'use strict'

const keys = require('./keys')

describe('keys()', () => {
  xit("returns an array of the given object's own keys", () => {})
  xit("does not include the given object's prototype properties", () => {})
  xit('works for primitives', () => {})
  xit('is curried', () => {})
})
