'use strict'

const assoc = require('./assoc')

describe('assoc()', () => {
  xit('sets the specified property', () => {})
  xit('overrides only the specified property', () => {})
  xit('is immutable', () => {})
  xit('is curried', () => {})
})
