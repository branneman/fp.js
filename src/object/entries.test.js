'use strict'

const entries = require('./entries')

describe('entries()', () => {
  xit('converts an object into an array of two-element [key, value] arrays', () => {})
  xit("only returns the object's own properties", () => {})
  xit('is curried', () => {})
})
