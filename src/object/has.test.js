'use strict'

const has = require('./has')

describe('has()', () => {
  xit('returns true if the specified property is present', () => {})
  xit('returns false if the specified property is absent', () => {})
  xit('does not check properties from the prototype chain', () => {})
  xit('returns false for non-objects', () => {})
  xit('is curried', () => {})
})
