'use strict'

const nth = require('./nth')

describe('nth()', () => {
  xit('returns match on array', () => {})
  xit('returns match on object', () => {})
  xit('returns `undefined` if no match was found', () => {})
  xit('is curried', () => {})
})
