'use strict'

const length = require('./length')

describe('length()', () => {
  xit('returns the length of a list', () => {})
  xit('returns the length of a string', () => {})
  xit('returns the length of a function', () => {})
  xit('is curried', () => {})
})
