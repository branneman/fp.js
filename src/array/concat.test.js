'use strict'

const concat = require('./concat')

describe('concat()', () => {
  xit('combines the elements of two lists', () => {})
  xit('works on strings', () => {})
  xit('is immutable', () => {})
  xit('is curried', () => {})
})
