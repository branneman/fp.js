'use strict'

const reverse = require('./reverse')

describe('reverse()', () => {
  xit('reverses arrays', () => {})
  xit('reverses strings', () => {})
  xit('is immutable', () => {})
  xit('is curried', () => {})
})
