'use strict'

const update = require('./update')

describe('update()', () => {
  xit('updates the value at the given index of the supplied array', () => {})
  xit('is immutable', () => {})
  xit('is curried', () => {})
})
