'use strict'

const range = require('./range')

describe('range()', () => {
  xit('returns list of numbers', () => {})
  xit('returns the empty list if first ≥ second', () => {})
  xit('is curried', () => {})
})
