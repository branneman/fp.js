'use strict'

const eq = require('./eq')

describe('eq()', () => {
  xit('considers equal Boolean primitives equal', () => {})
  xit('considers equal number primitives equal', () => {})
  xit('considers equal string primitives equal', () => {})
  xit('compares objects by reference', () => {})
  xit('is curried', () => {})
})
