'use strict'

const match = require('./match')

describe('match()', () => {
  xit('determines whether a string matches a regex', () => {})
  xit('throws on null input', () => {})
  xit('is immutable', () => {
    // defaults to a different empty array each time
  })
  xit('is curried', () => {})
})
