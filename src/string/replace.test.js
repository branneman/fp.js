'use strict'

const replace = require('./replace')

describe('replace()', () => {
  xit('replaces substrings of the input string', () => {})
  xit('replaces regex matches of the input string', () => {})
  xit('is curried', () => {})
})
