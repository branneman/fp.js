'use strict'

const test = require('./str-test')

describe('test()', () => {
  xit('returns true if string matches pattern', () => {})
  xit('returns false if string does not match pattern', () => {})
  xit('throws if first argument is not a regexp', () => {})
  xit('is immutable', () => {})
  xit('is curried', () => {})
})
